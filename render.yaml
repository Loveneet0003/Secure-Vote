services:
  - type: web
    name: secure-vote-api
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: node index.mjs
    envVars:
      - key: PORT
        value: 3001
      - key: MONGODB_URI
        sync: false
    autoDeploy: true
    healthCheckPath: /api/health 